<?xml version="1.0" ?>
<!DOCTYPE xml>
<project name="SkyRace" default="Build">
    <target name="Build">
        <jar jarfile="C:/MC server/plugins/${ant.project.name}.jar" basedir="./target/classes" includes="**/*">

        </jar>
    </target>
</project>